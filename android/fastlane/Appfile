# Fastlane Appfile for Google Play (supply) uploads.
# Fill these in once you have a Play Console account + service account JSON.

# The app's application id as registered on Google Play.
package_name("ai.exla.slide")

# Path to the Google Play service-account JSON key (gitignored).
# Create at: Play Console → Setup → API access → Service accounts.
json_key_file("fastlane/play-service-account.json")
